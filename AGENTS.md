# AGENTS.md — voice-input 工程约束

架构与技术决策(听写管线、引擎架构、权限与签名、分发模型、目录结构、编码约定、已知平台坑)已迁移至 [ARCHITECTURE.md](./ARCHITECTURE.md),开发时一并遵守其中【已确认】条目。

## 工具链约束

- **只用 Makefile 驱动构建**:`make setup`(模型)/ `make build`(裸二进制)/ `make app`(.app 打包+稳定签名)/ `make dist`(分发包)。不手敲 go build 绕过 Makefile 中的 CGO/签名/rpath 步骤。
- **CGO_ENABLED=1 永远必开**:sherpa-onnx、malgo、CGEvent 注入/热键全是 cgo,纯 Go 构建必然失败。
- **Go 位于 `/usr/local/go/bin/go`**(非交互 shell 的 PATH 里没有它;Makefile 已用 `GO :=` 绝对路径,手跑 go 命令时需自行 export PATH)。
- **国内网络**:`go mod tidy` 会回退直连 GitHub 拉模块元数据,直连不通会**无限卡住**——构建时带本地代理 `export https_proxy=http://127.0.0.1:7897 http_proxy=127.0.0.1:7897`(先用 `nc -z -w 3 127.0.0.1 7897` 探活;约定见 chat-elf 同款:命令级使用,不写入任何持久配置)。模型下载(hf-mirror)和百炼 API **不需要**代理,别画蛇添足。

## 依赖引入约束

新增任何依赖之前,必须先向用户说明并获得确认:

1. **为什么需要新依赖**——现有依赖或少量自研代码能否覆盖(本项目已有先例:为避开 robotgo 自研了 80 行 CGEvent 注入,为避开失灵的热键库自研了 CGEventTap);
2. **技术选型理由**——成熟度、体积、cgo 影响面、维护状态、与本仓库既有决策的契合度;
3. **cgo 依赖加倍谨慎**——每一个 cgo 依赖都会进入 .app 的链接与分发链路,优先纯 Go,其次带预编译库的平台模块(参照 sherpa-onnx-go-macos 模式)。

未经说明直接安装依赖视为违规操作。

## 语言与 CGO 约束

- 代码全部使用 Go;C 代码只允许放在对应 internal 包的 `.c`/`.h` 文件(参照 internal/hotkey/),不在 Go 注释块里堆长 C 代码;
- `//export` 注释名必须与 Go 函数名**完全一致**(cgo 硬规则,不一致直接编译错);
- 跨 goroutine 的共享状态一律 `atomic` 或 mutex(菜单/热键/音频三线并发是常态);cgo 回调内**永不阻塞**——只投递,处理交给 Go 侧 goroutine;
- 每个包保持单一职责,新能力先进 internal/ 再考虑对外。

## macOS 权限与调试约束(本项目特有,违者浪费数小时)

- **辅助功能/麦克风授权绑定代码签名**。临时(ad-hoc)签名每次编译指纹都变,授权会**静默失效**(面板显示开着但系统不认)。一切测试必须基于 `make app` 的稳定签名构建(`voiceinput-dev` 证书);**在授权与测试之间绝不重新构建**。
- **测试 .app 前必须 `make app`**——`make build` 只更新裸二进制,bundle 里还是旧的;对已运行的 .app 执行 `open` 只是激活不会重启,测试前先 `pkill -f "VoiceInput.app/Contents/MacOS"`。
- **`~/.voice_input/app.log` 被所有运行实例共享追加**——看到"诡异交错"的日志先查有没有残留实例(pkill),别急着改代码。
- 排障第一现场永远是 `tail ~/.voice_input/app.log`(标准 log 已重定向进文件);给用户转发的终端命令必须**零通配符、零 bashism**(zsh NOMATCH 会整条中止)。

## specCoding 规则

需求以规格(spec)驱动,规格文件集中放在项目根目录 `specCoding/`:

- **一个特性一个文件夹**,文件名即步骤计划(如 `流式听写/step1-本地流式.md`)。文件头标注状态(草案 / 已确认)与最近更新日期。
- **spec 的产生**:由规划会话调研后起草,与用户讨论完善;未达「已确认(含日期)」状态不得交给实施会话开工。
- **spec 必须自洽可交接**:实施会话可能从零上下文开始,spec 需覆盖:背景与目标、范围(做/明确不做)、现状盘点、任务分解(涉及文件、实现要点、验收标准)、依赖引入清单、待决策项。
- **实施会话的职责边界**:开工前按顺序读 AGENTS.md → ARCHITECTURE.md → `Memory/` 最新存档 → 对应 spec;只按 spec 实施并在「实施记录」小节追加进度;遇到 spec 未覆盖的决策点必须停下询问用户。
- **spec 的收尾**:全部任务完成并验收后,将文件夹移入 `specCoding/done/` 归档。

## 进度保存规则

每当用户说**保存进度**或执行 `git push` 时,立即在项目根目录 `Memory/` 文件夹中保存当前工作进度:

- 文件名为**当天日期**:`YYYY-MM-DD.memory.md`;当天文件已存在则**更新**它,不新建。
- 内容基于当前窗口上下文总结,面向"下一个接手的 Agent"书写,必须覆盖四块:
  1. **项目在做什么**:当前任务/里程碑的背景与目标;
  2. **已完成**:本轮关键改动(涉及文件、功能、技术决策);
  3. **未完成/下一步**:剩余工作、已知问题、待用户验证的事项;
  4. **环境与命令**:跑起来所需的关键信息(代理、路径、签名、易踩的坑)。
- 目的:团队换用任意 Agent(ZCode / Claude Code / Codex 等)时,读取 `Memory/` 最新存档即可同步上下文。接手会话应先看 `Memory/` 最新文件再开工。

## 回答与建议原则

对所有询问与问题:

- **第一性原则**:从基本事实和机制出发推理,先搜集足够的资料再综合回答,而非套用惯性结论。
- **不盲从、不趋附**:用户观点与客观事实或最佳实践冲突时,直接指出分歧并陈述依据。
- **不编造答案**:无法确认的事实明确说明"未确认"并给出验证途径。
- **文档查询一律求新**:涉及 API(百炼接口、sherpa-onnx 绑定、systray)必须核实最新文档或直接翻 `$(go env GOMODCACHE)` 里的真实源码——本项目已三次踩"博客写法过时"的坑,教训入档。
- **建议客观、有据、可溯源**:讲清事实依据,注明适用前提,引用来源。
